#  <#Title#>

```
struct ContentView: View {
    @State private var selectedTab = "One"

    var body: some View {
        Image(.example)
            .interpolation(.none)
            .resizable()
            .scaledToFit()
            .background(.black)
    }
    
   
   ```
